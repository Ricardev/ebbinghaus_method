import 'package:flutter/material.dart';

class AnswerCardPage extends StatelessWidget {
  const AnswerCardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: PageView.builder(
        itemCount: 4,
        itemBuilder: (context, index) =>)
    );
  }
}
